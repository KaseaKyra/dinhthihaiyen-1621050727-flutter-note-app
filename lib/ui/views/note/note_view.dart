import 'package:flutter/material.dart';

class NoteView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Flutter with Sqflite'),
      ),
      body: Text('body'),
    );
  }
}
